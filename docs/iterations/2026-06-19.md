# Iteration Logs: 2026-06-19


### Iteration 54 Detail — 2026-06-19

**Theme:** Architecture Alignment & Auth Hardening (Security & Architecture)

**What We Did:**
Hardened authentication using `bcrypt` and signed JWTs. Implemented BrokerSymbolTranslation layer using `BrokerSymbolMappings` in SQLite. Decoupled Quality Indicators and passed them into `evaluateRebalance` from the orchestrator. Expanded `updateModel` to cascade policy and constraints. Added idempotent `PUT /api/models/:id` endpoint and documented the mandate schema.

**Decisions & Rationales:**
Abstracting symbol mappings protects the pure evaluation loop from broker-specific ticker nuances. Quality indicator injection fulfills the DI design.

**Files Touched:**
`src/api/server.ts`, `src/db/sqlite.ts`, `src/orchestrator/sqlite-state.ts`, `src/broker/alpaca-broker.ts`, `src/core/evaluation.ts`, `docs/api/mandate-schema.md`

**Open Questions:**
None.

**Next Steps:**
Validate full orchestrator evaluation flow.


### Iteration 55 Detail — 2026-06-19

**Theme:** Native Portfolio Mandates & Týr Agent API (Architecture Alignment & API MVP)

**What We Did:**
Corrected architecture to store `archetype` and `constraints` natively on `Portfolios`. Modified `loop.ts` to evaluate portfolios independent of `Models` table lookups. Completed Týr Agent API requirements: prevented EfficientFrontier usage until implemented, validated `TargetAllocation` cash buffer sum, added `GET /api/models/:id` and `PUT /api/portfolios/:id/mandate`. Documented mandates in OpenAPI spec.

**Decisions & Rationales:**
Tying mandates directly to portfolio state avoids orphaned portfolios and correctly isolates bespoke versus discretionary configurations.

**Files Touched:**
`src/orchestrator/state.ts`, `src/db/sqlite.ts`, `src/orchestrator/sqlite-state.ts`, `src/orchestrator/loop.ts`, `src/api/server.ts`, `src/api/openapi.ts`, `src/cli/agent.ts`, `tests/*`

**Open Questions:**
None.

**Next Steps:**
Validate integration with Týr Agent.


### Iteration 56 Detail — 2026-06-19

**Theme:** Test Suite Review & Gap Fixes (Issue #27) (Testing & Quality)

**What We Did:**
Added missing E2E integration tests for model update fan-outs and B2B API key authentication lifecycle. Fixed SqliteStateManager foreign key ON DELETE cascade issue in ModelRepository UPSERT logic that was breaking target allocation updates. Corrected indicator names and instantiation dependencies (clock injection in MockOptimizerService) across test suites. Attained clean pass for all 195 test assertions.

**Decisions & Rationales:**
SQLite's `INSERT OR REPLACE` implicitly triggers `ON DELETE SET NULL` on foreign keys, wiping out portfolio assignments; `ON CONFLICT DO UPDATE` must be used.

**Files Touched:**
`tests/api.test.ts`, `tests/evaluation.test.ts`, `tests/integration.test.ts`, `tests/metrics.test.ts`, `tests/optimizer.test.ts`, `tests/orchestrator.test.ts`, `tests/sqlite-state.test.ts`, `src/db/repositories/ModelRepository.ts`

**Open Questions:**
None.

**Next Steps:**
Continue feature work.


### Iteration 57 Detail — 2026-06-19

**Theme:** Resolve Code Scanning Alerts (Security)

**What We Did:**
Checked GitHub Advanced Security alerts via `gh api` and resolved the open `actions/missing-workflow-permissions` alert by adding an explicit `permissions: contents: read` block to the CI workflow in `.github/workflows/ci.yml`.

**Decisions & Rationales:**
Workflows must explicitly limit the `GITHUB_TOKEN` scope to adhere to the principle of least privilege.

**Files Touched:**
`.github/workflows/ci.yml`

**Open Questions:**
None.

**Next Steps:**
-
