# Iteration Logs: 2026-07-01


### Iteration 65 Detail — 2026-07-01

**Theme:** Deployment Bug Fixes (Issues #45, #46) (Reliability & Perf)

**What We Did:**
Addressed post-deployment feedback and merged PR #47. Added `isRunning` lock to `BrokerSyncService` to prevent overlapping API calls. Created `readLinesBackwards` utility to asynchronously stream `audit-trail.jsonl` from the end of the file, replacing `fs.readFileSync` calls that caused memory exhaustion risk.

**Decisions & Rationales:**
Processing large text logs requires streaming; reading files backwards is efficient for latest-first API responses.

**Files Touched:**
`src/broker/sync.ts`, `src/api/server.ts`, `src/utils/fs.ts`

**Open Questions:**
None.

**Next Steps:**
Continue roadmap implementation.


### Iteration 66 Detail — 2026-07-01

**Theme:** Tranche 4 - Slice 1: Webhook & Execution Reconciliation (Live Trading Readiness)

**What We Did:**
Added `Orders` table to SQLite schema. Updated `LiveStateManager` and `SqliteStateManager` to register orders and process execution reports (updating status and incrementally updating Holdings/Cash based on partial fills). Created `setupBrokerWebhooks` router in `src/api/webhooks/broker-reports.ts` to parse Alpaca trade events.

**Decisions & Rationales:**
Real-world execution involves partial fills and rejections. Processing these accurately ensures local state doesn't drift from reality between full syncs.

**Files Touched:**
`src/db/sqlite.ts`, `src/orchestrator/state.ts`, `src/orchestrator/sqlite-state.ts`, `src/api/webhooks/broker-reports.ts`, `src/orchestrator/executor.ts`

**Open Questions:**
None.

**Next Steps:**
Tranche 4 - Slice 2 (Persistent Audit Trails).


### Iteration 67 Detail — 2026-07-01

**Theme:** Tranche 4 - Slices 2 & 3: Audit Trails & Webhook Alerts (Safety & Compliance)

**What We Did:**
Migrated audit trails from `audit-trail.jsonl` to SQLite `AuditTrails` table. Refactored API endpoints to query DB directly. Added `WebhookNotifier` for Slack/Discord integration and `ALERT_WEBHOOK_URL` to `.env.example`.

**Decisions & Rationales:**
Flat files exhaust memory/storage over time; SQLite provides robust, queryable compliance history. Critical alerts require external delivery paths outside the terminal output.

**Files Touched:**
`src/db/sqlite.ts`, `src/audit/storage.ts`, `src/api/server.ts`, `src/notifications/webhook.ts`, `src/cli/agent.ts`, `.env.example`

**Open Questions:**
None.

**Next Steps:**
Conclude MVP and prepare for Tranche 5 (User Feedback/Beta).


### Iteration 68 Detail — 2026-07-01

**Theme:** Tranche 4 - Slice 4: TCO Optimizer Un-Mocking (Configuration)

**What We Did:**
Parameterized `PercentageSlippageModel` via `assumedSlippageBps` in `RebalancingPolicy`, replacing the hardcoded 5 bps. Formally deferred live-spread fetching via ADR-0056.

**Decisions & Rationales:**
Provides flexibility for portfolios of varying liquidity without the heavy architectural burden of live Level-1 quote streams for the MVP.

**Files Touched:**
`src/models/domain.ts`, `src/orchestrator/loop.ts`, `docs/decisions/0056-deferred-live-spread-slippage.md`

**Open Questions:**
None.

**Next Steps:**
Tranche 5 roadmap alignment.


### Iteration 69 Detail — 2026-07-01

**Theme:** Tranche 5 - Slice 1: Advisor Workspace MVP (UI & UX)

**What We Did:**
Refactored the Command Center frontend to support Persona-specific routing. Created a dedicated `AdvisorLayout` with a Tailwind CSS sidebar. Revamped the `CommandCenterDashboard` (Fleet View) using Tailwind CSS to cleanly segment Action Required vs Watchlist portfolios. Seeded SQLite with `Advisor`, `Tenant Admin`, and `Compliance` roles.

**Decisions & Rationales:**
Eliminates cognitive overload for Advisors by shielding them from Admin-level metrics and raw JSON logs. Tailwind accelerates UI iteration.

**Files Touched:**
`web/src/App.tsx`, `web/src/components/AdvisorLayout.tsx`, `web/src/components/CommandCenterDashboard.tsx`, `web/tailwind.config.js`, `src/db/sqlite.ts`

**Open Questions:**
None.

**Next Steps:**
Tranche 5 - Slice 2 (Compliance MVP).
